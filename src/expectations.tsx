import { useEffect,useMemo,useState } from "react";
import { AppBar,Box,Chip,Container,FormControl,IconButton,InputLabel,MenuItem,Paper,Select,Stack,Tab,Tabs,Table,TableBody,TableCell,TableContainer,TableHead,TableRow,Toolbar,Typography } from "@mui/material";
import { Home as HomeIcon } from "@mui/icons-material";
import { CartesianGrid,Legend,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
import { useNavigate,useSearchParams } from "react-router-dom";

type Payload={metadata:{coverage:{from:string;to:string}};forecastColumns:string[];opinionColumns:string[];forecasts:unknown[][];opinions:unknown[][]};
const colors=["#f28c28","#4da3ff","#33c37d","#c084fc","#ef6b6b","#f2c94c"];
const labels:Record<string,string>={INF_GEN:"Inflación general",INF_SUB:"Inflación subyacente",GDP_REAL:"Crecimiento del PIB",FONDEO:"Tasa de fondeo",CETE28:"Cetes a 28 días",OTHER:"Otros indicadores",OP_ENV:"Percepción del entorno",OP_FACTORS:"Factores que obstaculizan el crecimiento",OP_COMP:"Condiciones de competencia"};
const fmt=(v:unknown)=>typeof v==="number"&&Number.isFinite(v)?v.toFixed(2):"—";
function objects(cols:string[],rows:unknown[][]){return rows.map(row=>Object.fromEntries(cols.map((c,i)=>[c,row[i]])) as Record<string,unknown>)}
export default function ExpectationsRadar(){const [data,setData]=useState<Payload|null>(null);const nav=useNavigate();const [p,setP]=useSearchParams();useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/expectativas-banxico.json`).then(r=>r.json()).then(setData)},[]);if(!data)return <Box className="loading">Cargando expectativas de especialistas…</Box>;const tab=Math.min(4,Math.max(0,Number(p.get("tab")||0)));const set=(k:string,v:string|number)=>{const n=new URLSearchParams(p);n.set(k,String(v));setP(n)};return <Box><AppBar position="sticky" color="transparent" elevation={0}><Toolbar><IconButton onClick={()=>nav("/")}><HomeIcon/></IconButton><Box sx={{flexGrow:1}}><img className="brand-logo header-logo" src={`${import.meta.env.BASE_URL}logo.png`}/></Box><Typography>Encuesta de expectativas</Typography></Toolbar></AppBar><Tabs value={tab} onChange={(_,v)=>set("tab",v)} variant="scrollable" className="tabs"><Tab label="Pronósticos"/><Tab label="Evolución y errores"/><Tab label="Percepción"/><Tab label="Factores y competencia"/><Tab label="Metodología"/></Tabs><Container maxWidth={false} sx={{py:3,px:{xs:1.5,md:3}}}>{tab===0&&<ForecastView data={data} historical={false} params={p} set={set}/>} {tab===1&&<ForecastView data={data} historical params={p} set={set}/>} {tab===2&&<OpinionView data={data} params={p} set={set} forced="OP_ENV"/>} {tab===3&&<FactorsView data={data} params={p} set={set}/>} {tab===4&&<Methodology data={data}/>}</Container></Box>}
function ForecastView({data,historical,params,set}:{data:Payload;historical:boolean;params:URLSearchParams;set:(k:string,v:string)=>void}){const rows=useMemo(()=>objects(data.forecastColumns,data.forecasts),[data]);const indicators=[...new Set(rows.map(r=>String(r.indicator_id)))].filter(x=>x!=="OTHER");const indicator=params.get("indicator")&&indicators.includes(params.get("indicator")!)?params.get("indicator")!:"INF_GEN";const filtered=rows.filter(r=>r.indicator_id===indicator);const surveys=[...new Set(filtered.map(r=>String(r.survey_date)))].sort();const survey=params.get("survey")&&surveys.includes(params.get("survey")!)?params.get("survey")!:surveys.at(-1)!;const periods=[...new Set(filtered.map(r=>String(r.period_id)))].sort();const period=params.get("period")&&periods.includes(params.get("period")!)?params.get("period")!:periods.at(-1)!;const shown=(historical?filtered.filter(r=>r.period_id===period):filtered.filter(r=>r.survey_date===survey)).sort((a,b)=>String(historical?a.survey_date:a.target_date).localeCompare(String(historical?b.survey_date:b.target_date)));const chart=shown.map(r=>({x:historical?r.survey_date:r.target_period,Mediana:r.median,Media:r.mean,Q1:r.q1,Q3:r.q3,Mínimo:r.min,Máximo:r.max,Realizado:r.actual_value}));const latest=shown.at(-1);return <Stack spacing={3}><Box><Typography variant="overline" color="primary">CONSENSO DE ESPECIALISTAS</Typography><Typography variant="h3">{historical?"Evolución, dispersión y error":labels[indicator]}</Typography></Box><Paper className="forecast-controls"><Picker label="Indicador" value={indicator} items={indicators} display={labels} onChange={v=>set("indicator",v)}/>{historical?<Picker label="Periodo objetivo" value={period} items={periods} onChange={v=>set("period",v)}/>:<Picker label="Encuesta" value={survey} items={surveys} onChange={v=>set("survey",v)}/>}</Paper>{latest&&<Box className="decision-metrics"><Metric label="Mediana" value={fmt(latest.median)}/><Metric label="Rango intercuartílico" value={fmt(latest.IQR)}/><Metric label="Respuestas" value={String(latest.n_responses??"—")}/><Metric label="Revisión mensual" value={fmt(latest.Revision_1M)}/><Metric label="Dato realizado" value={fmt(latest.actual_value)}/><Metric label="Error" value={fmt(latest.forecast_error)}/></Box>}<Chart data={chart} series={historical?["Mediana","Q1","Q3","Realizado"]:["Mediana","Media","Mínimo","Máximo"]}/><ForecastTable rows={shown}/></Stack>}
function OpinionView({data,params,set,forced}:{data:Payload;params:URLSearchParams;set:(k:string,v:string)=>void;forced?:string}){const rows=useMemo(()=>objects(data.opinionColumns,data.opinions),[data]);const indicator=forced??params.get("opinion")??"OP_ENV";const filtered=rows.filter(r=>r.indicator_id===indicator&&r.target_period!=="Número de respuestas");const questions=[...new Set(filtered.map(r=>String(r.forecast_type)))];const question=params.get("question")&&questions.includes(params.get("question")!)?params.get("question")!:questions[0];const selected=filtered.filter(r=>r.forecast_type===question);const series=[...new Set(selected.map(r=>String(r.target_period)))];const dates=[...new Set(selected.map(r=>String(r.survey_date)))].sort();const chart=dates.map(d=>({x:d,...Object.fromEntries(series.map(s=>[s,selected.find(r=>r.survey_date===d&&r.target_period===s)?.mean]))}));return <Stack spacing={3}><Box><Typography variant="overline" color="primary">OPINIÓN Y ENTORNO</Typography><Typography variant="h3">{labels[indicator]}</Typography></Box><Paper className="forecast-controls"><Picker label="Pregunta" value={question} items={questions} onChange={v=>set("question",v)}/></Paper><Chart data={chart} series={series.slice(0,12)}/></Stack>}
function FactorsView({data,params,set}:{data:Payload;params:URLSearchParams;set:(k:string,v:string)=>void}){const rows=useMemo(()=>objects(data.opinionColumns,data.opinions),[data]);const blocks=["OP_FACTORS","OP_COMP"];const indicator=params.get("opinion")&&blocks.includes(params.get("opinion")!)?params.get("opinion")!:"OP_FACTORS";const filtered=rows.filter(r=>r.indicator_id===indicator);const types=[...new Set(filtered.map(r=>String(r.forecast_type)))];const type=params.get("factorType")&&types.includes(params.get("factorType")!)?params.get("factorType")!:types[0];const selected=filtered.filter(r=>r.forecast_type===type);const latest=selected.map(r=>String(r.survey_date)).sort().at(-1)!;const ranking=selected.filter(r=>r.survey_date===latest).sort((a,b)=>Number(b.mean??-Infinity)-Number(a.mean??-Infinity)).slice(0,15);const dates=[...new Set(selected.map(r=>String(r.survey_date)))].sort().slice(-18);const names=ranking.slice(0,8).map(r=>String(r.target_period));return <Stack spacing={3}><Box><Typography variant="overline" color="primary">RIESGOS Y ESTRUCTURA</Typography><Typography variant="h3">{labels[indicator]}</Typography></Box><Paper className="forecast-controls"><Picker label="Bloque" value={indicator} items={blocks} display={labels} onChange={v=>set("opinion",v)}/><Picker label="Medición" value={type} items={types} onChange={v=>set("factorType",v)}/></Paper><Typography variant="h5">Ranking más reciente · {latest}</Typography><TableContainer component={Paper}><Table size="small"><TableHead><TableRow><TableCell>Factor o sector</TableCell><TableCell align="right">Valor</TableCell></TableRow></TableHead><TableBody>{ranking.map(r=><TableRow key={String(r.target_period)}><TableCell>{String(r.target_period)}</TableCell><TableCell align="right">{fmt(r.mean)}</TableCell></TableRow>)}</TableBody></Table></TableContainer><Typography variant="h5">Mapa de calor · últimos 18 levantamientos</Typography><Paper sx={{p:2,overflowX:"auto"}}><Box sx={{display:"grid",gridTemplateColumns:`minmax(260px,1fr) repeat(${dates.length},34px)`,gap:"2px",minWidth:900}}>{names.map(name=><><Typography key={`${name}-label`} variant="caption" sx={{pr:1}}>{name}</Typography>{dates.map(date=>{const value=Number(selected.find(r=>r.survey_date===date&&r.target_period===name)?.mean??0);const alpha=Math.min(.95,.12+Math.abs(value)/Math.max(1,...selected.map(r=>Math.abs(Number(r.mean??0)))));return <Box key={`${name}-${date}`} title={`${date}: ${fmt(value)}`} sx={{height:25,bgcolor:`rgba(242,140,40,${alpha})`}}/>})}</>)}</Box></Paper></Stack>}
function Chart({data,series=["Mediana","Media","Q1","Q3"]}:{data:Record<string,unknown>[];series?:string[]}){return <Paper sx={{p:2}}><ResponsiveContainer width="100%" height={480}><LineChart data={data}><CartesianGrid stroke="#282828"/><XAxis dataKey="x" minTickGap={35}/><YAxis/><Tooltip contentStyle={{background:"#111",border:"1px solid #555"}}/><Legend/>{series.map((s,i)=><Line key={s} dataKey={s} name={s} stroke={colors[i%colors.length]} dot={false} connectNulls/>)}</LineChart></ResponsiveContainer></Paper>}
function Picker({label,value,items,onChange,display={}}:{label:string;value:string;items:string[];onChange:(v:string)=>void;display?:Record<string,string>}){return <FormControl size="small" sx={{minWidth:230}}><InputLabel>{label}</InputLabel><Select label={label} value={value} onChange={e=>onChange(e.target.value)}>{items.map(x=><MenuItem value={x} key={x}>{display[x]??x}</MenuItem>)}</Select></FormControl>}
function Metric({label,value}:{label:string;value:string}){return <Paper className="decision-metric"><Typography variant="caption" color="text.secondary">{label}</Typography><Typography variant="h6">{value}</Typography></Paper>}
function ForecastTable({rows}:{rows:Record<string,unknown>[]}){return <TableContainer component={Paper}><Table size="small"><TableHead><TableRow><TableCell>Encuesta</TableCell><TableCell>Objetivo</TableCell><TableCell align="right">Media</TableCell><TableCell align="right">Mediana</TableCell><TableCell align="right">Q1</TableCell><TableCell align="right">Q3</TableCell><TableCell align="right">Mín.</TableCell><TableCell align="right">Máx.</TableCell><TableCell align="right">n</TableCell><TableCell align="right">Realizado</TableCell><TableCell align="right">Error</TableCell></TableRow></TableHead><TableBody>{rows.slice(-80).map((r,i)=><TableRow key={`${r.survey_date}-${r.period_id}-${i}`}><TableCell>{String(r.survey_date)}</TableCell><TableCell><Chip size="small" label={String(r.target_period)}/></TableCell>{["mean","median","q1","q3","min","max","n_responses","actual_value","forecast_error"].map(k=><TableCell key={k} align="right">{k==="n_responses"?String(r[k]??"—"):fmt(r[k])}</TableCell>)}</TableRow>)}</TableBody></Table></TableContainer>}
function Methodology({data}:{data:Payload}){return <Stack spacing={2}><Typography variant="overline" color="primary">FUENTE Y CONSTRUCCIÓN</Typography><Typography variant="h3">Metodología</Typography><Paper sx={{p:3}}><Typography>Fuente: Encuesta sobre las Expectativas de los Especialistas en Economía del Sector Privado, Banco de México.</Typography><Typography color="text.secondary" sx={{mt:1}}>Cobertura: {data.metadata.coverage.from} a {data.metadata.coverage.to}. Los archivos Excel permanecen como fuente de verdad y el sitio consume una extracción JSON validada.</Typography></Paper></Stack>}
